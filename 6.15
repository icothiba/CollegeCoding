If the random seed value is 2 and the input is:

3

the output is:

Tails
Heads
Tails

#include <iostream>
#include <cstdlib>
using namespace std;

string CoinFlip() {
    int randomValue = rand() % 2;
    if (randomValue == 1) {
        return "Heads";
    } else {
        return "Tails";
    }
}

int main() {
   int numFlips;
   
   
    cin >> numFlips;
   
   srand(2);

   for (int i = 0; i < numFlips; i++) {
        cout << CoinFlip() << endl;
    }


   return 0;
}
