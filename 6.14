If the input is:

7.6

the output is:

1.90


#include <iostream>
#include <iomanip>                 
using namespace std;

double LapsToMiles(double userLaps) {
    double miles = userLaps * 0.25;
    return miles;
}

int main() {
   double laps;

    
    cin >> laps;

    cout << fixed << setprecision(2);
    cout << LapsToMiles(laps) << endl;
    
   

   return 0;
}
