Using this code, if the input is -1, then the output is -2.58333.

#include <iostream>
using namespace std;

int main() {
   int x;
   double y;

   cin >> x;

   y = ((1.0 / 3.0) * x) + (x / 4.0) + (2.0 * x);
   
   cout << y << endl;

   return 0;
}
