 If the input is:

n Monday

the output is:

1 n

If the input is:

z Today is Monday

the output is:

0 z's


#include <iostream>
using namespace std;

int CalcNumCharacters(string userString, char userChar) {
    int count = 0;
    for (char c : userString) {
        if (c == userChar) {
            count++;
        }
    }
    return count;
} 

int main() {
   char userChar;
    string userString;

    cin >> userChar;

    cin.ignore();
    getline(cin, userString);

    int numOccurrences = CalcNumCharacters(userString, userChar);

    cout << numOccurrences << " " << userChar;
    if (numOccurrences != 1) {
        cout << "'s";
    }
    cout << endl;


   return 0;
}
